import 'package:flutter/material.dart';

class MealplannerScreen extends StatelessWidget {
  const MealplannerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Mealplanner Screen"),
    );
  }
}